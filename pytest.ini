[pytest]
testpaths = test
markers =
    retrieval: 检索层端到端集成测试（需要 embedding 模型 + FAISS 向量库；无环境时自动 skip）
    evaluation: 评估框架集成测试（需要向量库 + embedding 模型）
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
