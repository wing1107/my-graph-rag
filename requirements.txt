# myGraphRagProject - LangGraph Self-RAG
# Verified working on Python 3.12, CPU-only (no CUDA), 2026-06-08
#
# Install steps:
#   1. pip install -r requirements.txt
#   2. Install torch CPU (NOT from PyPI default, which ships CUDA builds):
#        pip install torch==2.4.1 torchvision==0.19.1 --index-url https://download.pytorch.org/whl/cpu

gradio==6.16.0
python-dotenv==1.2.2

# LangChain stack
langchain==1.3.4
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langchain-core==1.4.0
langchain-openai==1.2.2
langchain-huggingface==0.3.0

# LangGraph - Self-RAG core
langgraph==1.2.4

# OpenAI 2.x SDK
openai==2.41.0

zhipuai==2.1.5.20250825

# Alibaba DashScope SDK (Qwen)
dashscope==1.25.21

faiss-cpu==1.14.2
numpy==1.26.4

# torch/torchvision: install CPU builds separately (see header above)
# torch==2.4.1+cpu
# torchvision==0.19.1+cpu
sentence-transformers==3.1.1
transformers==4.44.2

chromadb==1.5.9

unstructured==0.22.31
# PyMuPDF wheel (cp310 abi3, works on 3.10+); download from https://pypi.org/project/PyMuPDF/#files
# then install locally: pip install pymupdf-*.whl
# PyMuPDF>=1.23.0

# OCR support for scanned PDFs
easyocr==1.7.2

websocket-client==1.9.0
requests==2.34.2

tiktoken==0.13.0

# FastAPI backend (api/fast_api.py)
fastapi==0.136.3
uvicorn==0.49.0

# NLP
nltk==3.9.4
jieba==0.42.1
PyYAML==6.0.3

# Hybrid retrieval
rank_bm25==0.2.2

# LangGraph session persistence (SqliteSaver is built-in, no extra dep needed)
